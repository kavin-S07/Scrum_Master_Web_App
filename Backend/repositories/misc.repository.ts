// LEGACY: This file previously contained duplicate repositories for leave,
// work log, standup, and notifications that have been split into dedicated
// files with proper transactional safety, socket integration, and atomic
// upserts. Kept as a placeholder to avoid breaking stale imports — it is
// intentionally empty.
export {};
